/*
Um iterator é um objeto que permite percorrer uma coleção um elemento por vez, controlando manualmente qual será o próximo elemento.

Você já usa isso sem perceber quando utiliza coisas como

for...of
EX:                const nomes = ["Gustavo", "João", "Maria"];

                        for (const nome of nomes) {
                            console.log(nome);
                        }

O JavaScript consegue fazer isso porque o array possui um iterador.

Podemos acessar esse iterador manualmente usando:
const iterador = nomes[Symbol.iterator]();
iterador.next();

Cada chamada retorna um objeto parecido com:
{
    value: "Gustavo",
    done: false
}

value → valor atual.
done → indica se terminou.
false = ainda existem elementos.
true = terminou


EX:
const nomes = ["Gustavo", "João"];

const iterador = nomes[Symbol.iterator]();

console.log(iterador.next());
console.log(iterador.next());
console.log(iterador.next());

resultado:
{ value: "Gustavo", done: false }
{ value: "João", done: false }
{ value: undefined, done: true }






for...of
↓
percorre automaticamente

Iterator + next()
↓
você controla manualmente o próximo elemento

o laço for in, interage sobre as propriedades enumeradas(propriedades que tem um valor  já atribuido)


Iterator: é um objeto que controla a passagem por uma coleção, entregando um elemento por vez. No mercado, você pode encontrar esse conceito em estruturas de dados e APIs que precisam percorrer dados de maneira controlada

Iterator Protocol: um padrão do JavaScript que define como um objeto fornece seus valores um por vez.
*/
